

<h1>All Reviews</h1>



<%= "Sort by Location" %><br>
<%= "Sort by Most Recent" %><br>

<table border="2">

<tr>
  <th>Username</th>
  <th>Lesson Name</th>
  <th colspan="3">Unit Location</th>
  <th colspan="4">Ratings (Qual. / Help / Frust. / Time) </th>
  <th>User's Progress</th>
</tr>

<% @reviews.each do |review| %>
<% location = review.lesson.unit_location.split("-").map(&:to_i) %>

  <tr>
   <td><%= link_to review.user.username, user_path(review.user.username) %></td>
   <td><%= link_to review.lesson.name, review %></td>
   <td><%= location[0] %></td>
   <td><%= location[1] %></td>
   <td><%= location[2] %></td>
   <td><%= review.rating.quality %></td>
   <td><%= review.rating.helpfulness %></td>
   <td><%= review.rating.frustration %></td>
   <td><%= review.rating.time_taken %></td>
  </tr>

<% end %>

</table>
<br>

<%= link_to "Create a New Review", new_review_path %>
